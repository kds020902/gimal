name: gimal
description: "Flutter overlay controller app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.5.0
  webview_flutter: ^4.12.0
  shared_preferences: ^2.5.5
  gimal_overlay_utils:
    path: packages/gimal_overlay_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
